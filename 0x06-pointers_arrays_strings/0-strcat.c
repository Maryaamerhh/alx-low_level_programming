#include "main.h"

/**
 * _strcat - a function that concatenates two strings.
 * @dest: concatenate to
 * @src: concatenate from
 *
 * Return: Always 0 (success)
 */
char *_strcat(char *dest, char *src)
{
	int a, b;

	for (a = 0; dest[a] != '\0'; a++)
	{
	}
		for (b = 0; src[a + b] = *src++ != '\0'; b++)
		{
		}

		return (dest);
}


