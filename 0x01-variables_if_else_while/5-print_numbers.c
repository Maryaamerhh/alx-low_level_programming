#include <stdio.h>

/**
 * main - printing all single digit numbers of base 10 from 0
 *
 * Return: Always 0.
 */
int main(void)
{
	int digit;

	for (digit = 0; digit < 20; digit++)
		printf("%d", digit);

	printf("\n");

		return (0);
}
